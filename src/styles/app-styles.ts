import { StyleSheet } from 'aphrodite/no-important';

const styles = StyleSheet.create({
  wrapper: {
    backgroundColor: '#ccc',
    width: '100%',
    height: 300,
    margin: 20,
  },
});

export default styles;