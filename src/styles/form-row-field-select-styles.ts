import { StyleSheet } from 'aphrodite/no-important';

const styles = StyleSheet.create({
  formSelect: {
    fontSize: '16px',
    height: '30px',
  },
  formOption: {
    fontSize: '16px',
  },
});

export default styles;